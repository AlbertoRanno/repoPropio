<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title class="titulo">Editar Película</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>

  <body>
    <figure>
      <img src="/img/logo-DH.png" alt="Logo Digital House" />
    </figure>
    <h1 class="titulo">Película a Editar: <%= Movie.title %></h1>
    <section class="formulario">
      <form action="/movies/edit/<%= Movie.id %>?_method=PATCH" method="POST">
        <label for="title">Title </label>
        <p>
          <input
            type="text"
            name="title"
            id="title"
            value="<%= Movie.title %>"
            required
          />
        </p>
        <label for="rating">Rating </label>
        <p>
          <input
            type="number"
            name="rating"
            id="rating"
            value="<%= Movie.rating %>"
            required
          />
        </p>
        <label for="awards">Awards </label>
        <p>
          <input
            type="number"
            name="awards"
            id="awards"
            value="<%= Movie.awards %>"
            required
          />
        </p>
        <label for="release_date">Release date </label>
        <p>
          <input
            type="date"
            name="release_date"
            id="release_date"
            value="<%= Movie.release_date %>"
            required
          />
        </p>
        <label for="length">Length </label>
        <p>
          <input
            type="number"
            name="length"
            id=""
            value="<%= Movie.length %>"
            required
          />
        </p>
        <label for="genre_id">Genero </label>
        <p>
          <select name="allGenres" id="">
            <% for( let i = 0; i < allGenres.length; i++ ) { %> <% if
            (allGenres[i].id == Movie.genre_id) { %>
            <option value="<% allGenres[i].id %>" selected>
              <%= allGenres[i].name %>
            </option>
            <% } else { %>
            <option value="<% allGenres[i].id %> ">
              <%= allGenres[i].name %>
            </option>
            <% } %> <% } %>
          </select>
        </p>

        <br />
        <p>
          
            <button class="botonAgregar" type="submit">Actualizar</button>
       
        </p>

        <p>
          <button class="botonAgregar" type="submit">
            <a href="/movies/delete/<%= Movie.id %>">Eliminar</a>
          </button>
          <br />
        </p>

        <p>
          <button class="botonAgregar" type="submit">
            <a href="/movies">Listado de Películas</a>
          </button>
        </p>
      </form>
    </section>
  </body>
</html>
